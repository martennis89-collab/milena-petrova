# milenapetrova.bg backend — runtime dependencies
# Slimmed from the Emergent export: only packages the code actually imports.
# Version pins carried over from the working Emergent build. pip resolves the rest.

# Web framework / server
fastapi==0.110.1
uvicorn==0.25.0
starlette==0.37.2
pydantic==2.12.5
email-validator==2.3.0
python-multipart==0.0.24
python-dotenv==1.2.2

# MongoDB (async)
motor==3.3.1
pymongo==4.5.0
dnspython==2.8.0            # required for mongodb+srv:// Atlas connection strings

# Webinar reminder scheduler
APScheduler==3.11.3
pytz==2026.2
tzlocal==5.4.4

# HTTP + transactional email
requests==2.33.1
resend==2.27.0

# Google Calendar integration
google-api-python-client==2.194.0
google-auth==2.49.1
google-auth-oauthlib==1.3.1
google-auth-httplib2==0.3.1
