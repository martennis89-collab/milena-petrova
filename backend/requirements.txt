# Runtime dependencies for the milenapetrova.bg backend.
#
# This file lists direct dependencies only; pip resolves the rest. It replaces
# an earlier `pip freeze` of the Emergent base image, which carried ~140
# packages (litellm, openai, google-genai, boto3, pandas, numpy, tiktoken,
# huggingface_hub and the linters) that no code here imports, plus
# `emergentintegrations`, which is only available from a private index.
#
# Development and test tooling lives in requirements-dev.txt.

# --- Web framework -----------------------------------------------------------
fastapi==0.110.1
starlette==0.37.2
uvicorn==0.25.0
pydantic==2.12.5
email-validator==2.3.0        # pydantic EmailStr, used by routes/webinar.py

# --- Database ----------------------------------------------------------------
motor==3.3.1
pymongo==4.5.0
# pymongo prefers its pyOpenSSL TLS backend whenever pyOpenSSL is importable,
# which drags in cryptography (and its cffi backend). Pinned explicitly so the
# import does not fall through to a distro-managed build that may be incomplete.
cryptography==46.0.7
cffi==2.0.0

# --- Configuration and scheduling --------------------------------------------
python-dotenv==1.2.2
APScheduler==3.11.3           # webinar reminder scheduler
pytz==2026.2

# --- Third-party integrations ------------------------------------------------
requests==2.33.1              # Calendly and Meta Conversions API calls
resend==2.27.0                # transactional email
stripe==15.0.1                # payments (see utils/stripe_checkout.py)

# --- Google Calendar ---------------------------------------------------------
google-api-python-client==2.194.0
google-auth==2.49.1
google-auth-httplib2==0.3.1
google-auth-oauthlib==1.3.1
